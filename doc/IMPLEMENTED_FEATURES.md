# bevy_alight_motion 已实现功能文档

> **参考目标版本**：此仓库的 AM 工程与调试参考目标版本均为 **Alemon-Android Preview7.5 4.3.4.3019 (43475)**。
>
> Alemon 是 AM 的社区版本，可能会与官方版本存在差异，请注意。

本文档详细说明 `bevy_alight_motion` 目前已实现的 Alight Motion (AM) 功能。

如果您发现功能缺失或有误，请提交 Issue 或 PR。

## 项目概述

`bevy_alight_motion` 是一个 Bevy 插件，用于加载和播放 Alight Motion 项目文件 (`.amproj`)。它解析 AM 的 XML 结构，并在 Bevy
引擎中重现动画效果。

---

## 图层类型支持

| 图层类型     | AM 标签          | 说明                  | 完成 | Issue/PR |
|----------|----------------|---------------------|:--:|----------|
| **形状**   | `<shape>`      | 矩形、圆形，支持颜色填充和媒体填充   | ✅  | #1       |
| **空对象**  | `<nullobj>`    | 不可见的父级控制器，用于分组和层级控制 | ✅  | #1       |
| **嵌入场景** | `<embedScene>` | 预合成/子场景，支持嵌套和时间偏移   | ✅  | #1       |
| **图像**   | `<image>`      | 独立图像图层（类似媒体填充的形状）   | ✅  | #1       |
| **文本**   | `<text>`       | 基础文本渲染，支持自定义字体      | ✅  | #1       |
| **书签**   | `<bookmark>`   | 仅解析，不影响渲染           | ⚠️ | -        |
| **音频**   | `<audio>`      | 仅解析，暂不播放            | ❌  | -        |
| **相机**   | `<camera>`     | 仅解析，暂不支持相机效果        | ❌  | -        |
| **视频**   | `<video>`      | 仅解析，暂不支持视频播放        | ❌  | -        |

### 嵌入场景（编组）分辨率支持

编组 (`<embedScene>`) 支持自定义分辨率，内部场景的 `width` 和 `height` 属性定义了编组的内部画布尺寸。超出该尺寸的内部图层内容将被裁剪。

**实现特性**：

- 支持不同分辨率的编组（如 500x500、1600x900 等）
- 编组分辨率限制在本库中始终正确生效，无论编组是否有旋转/缩放变换

**AM 已知 Bug 说明**：

在 AM 中发现，编组分辨率原本应该限制住内部图层的显示范围。但是如果对编组使用旋转变换，在接近 45
度的倍数时，这个限制范围会被错误地扩大为一个正方形区域。

此 Bug 在以下版本中均得到了复现：

- Alemon-Android Preview7.5 4.3.4.3019 (43475)
- Alemon 提供的 Alight Motion 类原生-Android 5.0.249.1002172 MIX3 (1002172)

我们认为这是 AM 自身的 Bug，本库不刻意还原该行为。本库的实现是：**无论编组旋转与否，分辨率限制都正确生效**。

---

## 颜色和填充

| 功能          | AM 属性                           | 说明            | 完成 | Issue/PR |
|-------------|---------------------------------|---------------|:--:|----------|
| **颜色填充**    | `fillType="color"`              | SDF 渲染或精灵着色   | ✅  | #1       |
| **媒体填充**    | `fillType="media"`              | 图像纹理加载        | ✅  | #1       |
| **填充颜色**    | `<fillColor value="#AARRGGBB">` | LinearRgba 颜色 | ✅  | #1       |
| **填充颜色动画**  | `<fillColor><kf>`               | 关键帧插值         | ✅  | #1       |
| **JPEG 图像** | `.jpg` / `.jpeg`                | bevy 图像加载器    | ✅  | #1       |
| **PNG 图像**  | `.png`                          | bevy 图像加载器    | ✅  | #1       |
| **渐变填充**    | -                               | 线性/径向渐变       | ❌  | -        |
| **图案填充**    | -                               | 重复图案          | ❌  | -        |

---

## 边框和阴影

| 功能       | AM 属性                  | 说明                            | 完成 | Issue/PR |
|----------|------------------------|-------------------------------|:--:|----------|
| **边框宽度** | `<path-stroke><size>`  | SDF 描边                        | ✅  | #1       |
| **边框颜色** | `<path-stroke><color>` | 打包到着色器参数                      | ✅  | #1       |
| **圆角连接** | `join="round"`         | `stroked_fill_box.wgsl`       | ✅  | #1       |
| **斜角连接** | `join="miter"`         | `stroked_fill_box_miter.wgsl` | ✅  | #1       |
| **斜切连接** | `join="bevel"`         | `stroked_fill_box_bevel.wgsl` | ✅  | #1       |
| **圆形描边** | `.circle` 形状           | `stroked_fill_circle.wgsl`    | ✅  | #1       |
| **阴影效果** | -                      | 投影阴影                          | ❌  | -        |
| **发光效果** | -                      | 外发光/内发光                       | ❌  | -        |
| **虚线边框** | -                      | 虚线样式                          | ❌  | -        |

### 技术说明

描边使用 SDF（有向距离场）渲染，通过 `bevy_smud` 库实现。描边宽度在缩放动画中保持不变，与 AM 行为一致。

---

## 混合和不透明度

| 功能           | AM 属性                   | 说明       | 完成 | Issue/PR |
|--------------|-------------------------|----------|:--:|----------|
| **不透明度**     | `<opacity value="...">` | Alpha 通道 | ✅  | #1       |
| **不透明度动画**   | `<opacity><kf>`         | 关键帧插值    | ✅  | #1       |
| **遮罩层**      | `blending="mask"`       | 矩形裁剪     | ⚠️ | #1       |
| **混合模式**     | -                       | 叠加、正片叠底等 | ❌  | -        |
| **复杂形状遮罩**   | -                       | 非矩形遮罩    | ❌  | -        |
| **Alpha 遮罩** | -                       | 灰度遮罩     | ❌  | -        |

### 遮罩技术说明

当前遮罩实现使用矩形裁剪区域，通过 `MaskedSpriteMaterial` 着色器在片段着色器中丢弃遮罩区域外的像素。

---

## 移动和转换

| 功能        | AM 属性                      | 说明                           | 完成 | Issue/PR |
|-----------|----------------------------|------------------------------|:--:|----------|
| **位置**    | `<location value="x,y,z">` | Transform.translation        | ✅  | #1       |
| **旋转**    | `<rotation value="deg">`   | Transform.rotation (Z轴)      | ✅  | #1       |
| **缩放**    | `<scale value="sx,sy">`    | Transform.scale / SDF params | ✅  | #1       |
| **锚点/枢轴** | `<pivot value="px,py">`    | Anchor + 位置补偿                | ✅  | #1       |
| **位置动画**  | `<location><kf>`           | 关键帧插值                        | ✅  | #1       |
| **旋转动画**  | `<rotation><kf>`           | 关键帧插值                        | ✅  | #1       |
| **缩放动画**  | `<scale><kf>`              | 关键帧插值                        | ✅  | #1       |
| **锚点动画**  | `<pivot><kf>`              | 关键帧插值                        | ✅  | #1       |
| **3D 旋转** | X/Y 轴旋转                    | 透视变换                         | ❌  | -        |

### 坐标系统转换

- **AM**: 原点在左上角，Y 轴向下增加
- **Bevy**: 原点在中心，Y 轴向上增加

转换公式：

```rust
bx = am_x - canvas_width / 2.0
by = canvas_height / 2.0 - am_y
```

---

## 编辑形状

| 形状类型      | AM 标识                        | 说明          | 完成 | Issue/PR |
|-----------|------------------------------|-------------|:--:|----------|
| **矩形**    | `s=".rect"`                  | SDF 盒子 / 精灵 | ✅  | #1       |
| **圆形**    | `s=".circle"`                | SDF 圆形      | ✅  | #1       |
| **椭圆**    | `.circle` + 非均匀缩放            | SDF 椭圆      | ✅  | #1       |
| **尺寸属性**  | `<property name="size">`     | 半尺寸参数       | ✅  | #1       |
| **尺寸动画**  | `<property name="size"><kf>` | 关键帧插值       | ✅  | #1       |
| **星形**    | `s=".star"`                  | 预设形状        | ❌  | -        |
| **多边形**   | -                            | 其他预设形状      | ❌  | -        |
| **自定义路径** | -                            | 贝塞尔曲线       | ❌  | -        |
| **圆角矩形**  | -                            | 圆角半径        | ❌  | -        |

---

## 效果

效果是 AM 的核心功能之一。每个效果都有多个属性，下面按效果分组列出各属性的实现状态。

### Transform2 效果 (变换)

| 属性       | AM 参数  | 说明      | 完成 | Issue/PR |
|----------|--------|---------|:--:|----------|
| **X 偏移** | `posx` | 额外的水平位移 | ✅  | #1       |
| **Y 偏移** | `posy` | 额外的垂直位移 | ✅  | #1       |

**概况**： 仅实现了位置偏移属性。

**效果说明**：可以复制类似变换、角度、缩放的设置

### Wipe2 效果 (擦拭)

| 属性     | AM 参数     | 说明               |   完成    | Issue/PR |
|--------|-----------|------------------|:-------:|----------|
| **起始** | `start`   | 可见范围起点 (0.0-1.0) |    ✅    | #1       |
| **结束** | `end`     | 可见范围终点 (0.0-1.0) |    ✅    | #1       |
| **角度** | `angle`   | 擦除方向角度           |    ✅    | #1       |
| **羽化** | `feather` | 边缘柔和度            | ⚠️ 基本支持 | #1       |

**概况**： 羽化效果尚未进行校对。

**效果说明**： 从图层的相对两侧遮盖矩形片段。使用关键帧动画创建擦拭过渡。

### 拉伸片段效果 (Stretch Segment)

| 属性     | AM 参数     | 说明      |   完成    | Issue/PR |
|--------|-----------|---------|:-------:|----------|
| **拉伸** | `stretch` | 拉伸量（像素） |    ✅    | #1       |
| **角度** | `angle`   | 分割线角度   | ⚠️ 基本支持 | #1       |
| **偏移** | `offset`  | 分割线位置偏移 | ⚠️ 基本支持 | #1       |
| **平滑** | `smooth`  | 边缘平滑度   |    ❌    | -        |

**概况**： angle 和 offset 属性的实现存在轻微视觉差异，smooth 属性尚未实现。

**效果说明**：UV 域变形效果，沿分割线拉伸图像。

**拉伸公式：**

```
base_divisor = orig_width / 5.76
stretch_factor = 1.0 + stretch_px / base_divisor
new_width = orig_width * stretch_factor
```

**角度实现说明**：使用像素空间坐标旋转方法，通过顶点变换模拟计算精确的 AABB 包围盒。目前与 AM 原始效果存在轻微视觉差异。

### 未实现效果

| 效果       | AM ID | 说明         | 完成 | Issue/PR |
|----------|-------|------------|:--:|----------|
| **模糊**   | -     | 高斯模糊       | ❌  | -        |
| **色彩调整** | -     | 亮度/对比度/饱和度 | ❌  | -        |
| **发光**   | -     | 外发光/内发光    | ❌  | -        |
| **阴影**   | -     | 投影阴影       | ❌  | -        |

---

## 关键帧和缓动

| 功能        | AM 格式                         | 说明      | 完成 | Issue/PR |
|-----------|-------------------------------|---------|:--:|----------|
| **关键帧时间** | `t="0.0-1.0"`                 | 归一化时间插值 | ✅  | #1       |
| **关键帧值**  | `v="..."`                     | 多种类型解析  | ✅  | #1       |
| **线性缓动**  | 默认                            | 线性插值    | ✅  | #1       |
| **步进缓动**  | `e="step x y"`                | 离散跳跃    | ✅  | #1       |
| **贝塞尔缓动** | `e="cubicBezier x1 y1 x2 y2"` | 三次贝塞尔求解 | ✅  | #1       |

### 缓动实现

三次贝塞尔缓动使用牛顿迭代法求解：

```rust
fn cubic_bezier_y_for_x(x: f32, x1: f32, y1: f32, x2: f32, y2: f32) -> f32
```

---

## 其他功能

| 功能         | 说明                  | 完成 | Issue/PR |
|------------|---------------------|:--:|----------|
| **项目加载**   | 解压 .amproj (ZIP 格式) | ✅  | #1       |
| **XML 解析** | 解析 scene.xml        | ✅  | #1       |
| **媒体资源提取** | 从项目包加载图像            | ✅  | #1       |
| **字体加载**   | 加载自定义字体             | ✅  | #1       |
| **图层生命周期** | 基于时间显示/隐藏图层         | ✅  | #1       |
| **父子层级**   | 变换继承                | ✅  | #1       |
| **播放控制**   | 播放/暂停/循环/速度         | ✅  | #1       |
| **调试功能**   | 热重载、检查器集成           | ✅  | #1       |

---

## 示例文件对照

以下列出 `assets/am/` 目录中的示例文件及其对应的功能验证：

### 基础示例 (basic_*)

| 示例文件                                | 验证功能                   | 状态 |
|-------------------------------------|------------------------|:--:|
| `basic_pivot.amproj`                | 锚点/枢轴功能、位置补偿           | ✅  |
| `basic_shape.amproj`                | 矩形和圆形渲染、颜色填充、媒体填充、描边   | ✅  |
| `basic_resolution_group.amproj`     | 编组分辨率限制、编组旋转/缩放        | ✅  |
| `basic_resolution_group_ex.amproj`  | 编组分辨率限制的扩展测试           | ✅  |
| `basic_resolution_group_ex2.amproj` | 针对编组旋转 显示范围扩大 bug的专项测试 | ⚠️ |

### 效果示例 (fx_*)

| 示例文件                              | 验证功能          | 状态 |
|-----------------------------------|---------------|:--:|
| `fx_1_stretch_segment.amproj`     | 拉伸片段效果基本功能    | ✅  |
| `fx_1_ex_stretch_segment.amproj`  | 拉伸片段效果扩展测试    | ✅  |
| `fx_1_ex2_stretch_segment.amproj` | 拉伸片段效果角度/偏移测试 | ✅  |
| `fx_2_gaussian_blur.amproj`       | 高斯模糊效果        | ❌  |
| `fx_3_grid.amproj`                | 网格效果          | ❌  |

---

## 架构说明

### RTT (Render-to-Texture) 架构

本库采用 RTT 架构来支持效果渲染和编组裁剪：

1. **UnifiedEffectMaterial** - 统一效果材质，在单个 pass 中处理遮罩、擦拭、拉伸片段等效果
2. **EmbedClipMaterial** - 编组裁剪材质，用于编组场景的 OBB 裁剪
3. **PingPongBuffer** - 双缓冲系统，为复杂多 pass 效果预留（目前未使用）

### 渲染方式

| 情况              | 渲染方式                    |
|-----------------|-------------------------|
| 颜色填充 + 无描边      | Bevy Sprite             |
| 颜色填充 + 有描边      | SDF (bevy_smud)         |
| 圆形 (任何填充)       | SDF (bevy_smud)         |
| 媒体填充            | Bevy Sprite             |
| 媒体填充 + 效果       | UnifiedEffectMaterial   |
| 编组 (embedScene) | EmbedClipMaterial + RTT |

### 效果叠加策略

当前实现使用 `UnifiedEffectMaterial` 在单个 shader pass 中处理多种效果组合：

- 遮罩裁剪 (Mask)
- 擦拭过渡 (Wipe)
- 拉伸片段 (Stretch Segment)

对于编组场景 (embedScene)：

- 使用独立的 RTT 相机渲染编组内容
- 通过 RenderLayers 实现图层隔离
- 使用 `EmbedClipMaterial` 进行 OBB 裁剪

---

## 相关文件

- `src/schema.rs` - AM XML 数据结构定义
- `src/scene.rs` - 场景构建和实体生成
- `src/animation.rs` - 动画系统和效果渲染
- `src/effects.rs` - RTT 架构和 PingPong 缓冲
- `src/sdf.rs` - SDF 形状渲染
- `src/masked_sprite.rs` - UnifiedEffectMaterial 定义
- `assets/shaders/unified_effect.wgsl` - 统一效果着色器
- `assets/shaders/embed_clip.wgsl` - 编组裁剪着色器
