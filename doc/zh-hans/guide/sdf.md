# SDF 渲染

有向距离场 (SDF) 是我们形状渲染系统的核心。

## 优势

- **分辨率无关**：即使无限放大，形状边缘依然保持完美清晰。
- **高效描边**：可以渲染具有不同宽度和连接样式（圆角、斜接、斜切）的边框，而无需额外的几何体。
- **抗锯齿**：通过片段着色器中的数学计算实现完美的边缘抗锯齿。

## 实现方式

我们使用自定义 WGSL 着色器（如 `sdf_shape.wgsl`），计算当前像素到形状边界的距离。

### 支持特性
- 支持独立圆角的矩形。
- 圆形和椭圆。
- 动态描边宽度。
